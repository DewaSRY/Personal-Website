import SmoothScrollSection from "./smooth-scroll-section";
import Provider, { useProvider as useSmoothScroll } from "./provider";

const SmoothScroll = {
  SmoothScrollSection,
  Provider,
};

export default SmoothScroll;
export { useSmoothScroll };
